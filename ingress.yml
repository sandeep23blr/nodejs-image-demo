apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-shark
spec:
  rules:
  - host: "foo.bar.com"
    http:
      paths:
      - pathType: Prefix
        path: "/bar"
        backend:
          service:
            name: sharkservice
            port:
              number: 80

